import { createTheme} from '@mui/material';

export const theme = createTheme({
    palette: {
      primary: {
        main: '#1976d2',
      },
    },
    typography: {
        fontFamily: `'Roboto', 'Arial', sans-serif`, // Replace with your custom font name
      },
});